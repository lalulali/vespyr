# Security Red-Team Corpus

Labeled attack fixtures for `security-scan.js`. Every rule has >=1 positive + >=1 negative fixture (per-rule invariant, Nina).

> [!NOTE]
> Attack strings below are deliberately **broken with zero-width spaces** (`​`) so
> the README does not self-flag when the corpus root is scanned (F-10). The
> fixtures themselves contain the real payloads.

**Invariants (CI-enforced by `node .agents/scripts/check_corpus_invariants.js`):**
- Every rule in `audit-spec.json` has >=1 positive fixture (in `positive/`) and >=1 negative fixture (in `negative/`).
- Positive fixtures MUST produce a finding; negative fixtures MUST NOT (exit 0 on a negative-only scan).
- Negative fixtures include: fake-harness name (must not match pattern table), low-entropy secret (regex matches, entropy < threshold -> NOT a finding).
- Corpus files are scanned with `security-scan.js --dir eval/security/corpus --spec artifacts/docs/strategy/development-plan/security/audit-spec.json`.

## Rule -> fixture map

| Rule | Label | Positive | Negative |
|---|---|---|---|
| GH-1 | INJ-HOOK | `positive/gh1/.git/config` with `core.hooksPath` + `positive/gh1/.husky/pre-commit` | `negative/gh1/.git/config` without hooks entries |
| INJ-PROMPT | INJ-PROMPT | `positive/inj-prompt.md` (skill body directive) | `negative/inj-prompt.md` (benign typo phrasing) |
| INJ-TOOL | INJ-TOOL | `positive/inj-tool.md` (fabricated invoke block in prose) | `negative/inj-tool.md` (prose mentioning the word) |
| INJ-OBFUSC | INJ-OBFUSC | `positive/inj-obfusc.md` (base64 blob >=40 chars, mixed case + digits) | `negative/inj-obfusc.md` (short/low-entropy token) |
| INJ-ROLE | INJ-ROLE | `positive/inj-role.md` (system-role override phrasing) | `negative/inj-role.md` (legit persona line) |
| INJ-TEMPLATE | INJ-TEMPLATE | `positive/templates/user-query.template.md` (`{{user_input}}` into code) | `negative/templates/doc-placeholder.template.md` (spaced brace doc placeholder) |
| INJ-PATH | INJ-PATH | `positive/inj-path;traversal.md` (metacharacter/traversal-shaped filename) | `negative/inj-path/budget-report.md` (prose with `$`; filename-scope rule must not fire) |
| INJ-SECRET | INJ-SECRET | `positive/inj-secret.md` (high-entropy `ghp_` token) | `negative/inj-secret.md` (low-entropy `AKIA`+repeats) |
| INJ-CONFIG | INJ-CONFIG | `positive/opencode.json` (with `mcpServers`) | `negative/opencode.json` (no mcp keys) + `negative/harness-shaped-dir/` (fake harness) |
| INJ-SYMLINK | INJ-SYMLINK | `positive/inj-symlink-escape` (target outside repo) | `negative/inj-symlink-inside.md` (target inside repo) |
| BEACON-1 | S16-BEACON | `positive/beacon-1.sh` (curl-pipe to sh) | `negative/beacon-1.sh` (curl without pipe-to-shell) |

## Known-FP notes

- **INJ-OBFUSC (base64-blob heuristic) is FP-prone on long tokens** (02f §10,
  supply-chain-audit-spec §7). Scanner suppression layers (F-3, Victor):
  1. token-fragment guard — a purely base64-class match immediately preceded
     on the line by two token-continuation chars (`[A-Za-z0-9_-]`) is a
     fragment of a longer credential-shaped token (e.g. the `ghp_` token in
     `positive/inj-secret.md` fires **only** INJ-SECRET, never INJ-OBFUSC);
  2. structural guard — a match must contain a digit or `+/=`, mix upper/lower
     case, and contain neither `//` nor `.` to count as an obfuscation blob
     (URLs, absolute paths, camelCase-acronym runs are excluded).
  Remaining documented FP classes (3 findings on `.agents`, accepted):
  camelCase-acronym word runs joined by single `/`, URL fragments, long POSIX
  paths. A corpus negative for the word-run class is under the growth rules.
- **INJ-PATH is filename-scoped** (supply-chain-audit-spec §7, line 183):
  applied to filenames and interpolated identifiers only, never full prose —
  `$`/parens in natural language are NOT findings.

## Frozen baseline

- **`baseline-2026-08-10.json`** — first clean baseline after the F-1/F-2/F-3/F-4
  scanner fixes and corpus rebuild (14 findings, all 10 rules firing, deterministic).
  FP gate = NEW-FINDINGS-ONLY vs this frozen baseline; never auto-baselined.
  Scanner fingerprint: `scanner_sha256_prefix` field in the baseline file.
- Generated by: `security-scan.js --dir eval/security/corpus --spec <audit-spec> --json`
- Enforced by: `node .agents/scripts/check_corpus_invariants.js` (CI gate).
